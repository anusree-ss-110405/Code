import converter
from converter import kg_to_lbs
print(converter.kg_to_lbs(70))

from max_num_in_array import find_max
numbers = [23,56,89,456,789]
maxi = find_max(numbers)
print(maxi)
print (max(numbers))